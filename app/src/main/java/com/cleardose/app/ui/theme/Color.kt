package com.cleardose.app.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Pastel / Soft Blue Palette ("soft blue" / "clean pastel" aesthetic)
val DeepBlack = Color(0xFFF0F4F8)      // Clean cloud grey-blue background
val NavyBlue = Color(0xFFE1EEFF)       // Soft baby blue (secondary background / gradient end)
val NeonCyan = Color(0xFFA2D2FF)       // Soft pastel baby blue (Primary Accent)
val NeonGreen = Color(0xFFB5E2C4)      // Soft pastel sage green (success)
val NeonBlue = Color(0xFF468FAF)       // Clean slate blue (primary button / active)
val NeonRed = Color(0xFFE5A4A4)        // Soft pastel warning red
val NeonGold = Color(0xFFFFD1A9)       // Soft pastel gold / premium

val GlassWhite = Color(0xCCFFFFFF)     // 80% Opacity white for premium light glassmorphism
val SmokeGrey = Color(0xFF3B4E61)      // Muted slate grey for subtext / secondary labels (High-contrast slate)
val Charcoal = Color(0xFF1E2D3D)       // Deep slate charcoal for primary readable text
val TextColor = Charcoal

val CardBgColor = Color(0xF2FFFFFF)    // 95% opaque white for clean pastel cards
val CardBorderColor = Color(0x1F1E2D3D) // 12% opacity Charcoal for soft borders

// High-contrast text colors for light background readability
val TextCyan = Color(0xFF2A6B8A)
val TextBlue = Color(0xFF2A6B8A)
val TextGreen = Color(0xFF2E6D4E)
val TextRed = Color(0xFFC24141)
val TextGold = Color(0xFF9E642A)

// Legacy aliases for compatibility
val MintGreen = NeonGreen
val AccentCyan = NeonCyan
val PrimaryBlue = NeonBlue
